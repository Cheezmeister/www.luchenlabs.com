extends ./page.jade

block innercontent

  .content !=content

  table.tunes
    each t in data.tracks
      tr
        td(id='#{t.title}')
          h3: a(href='#{t.path}') #{t.title}
          p #{t.description}
        td
          audio(src='#{t.path}' controls) Looks like your browser can't play HTML5 audio.


